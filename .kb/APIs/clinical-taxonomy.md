# API — `clinical-taxonomy`

<!-- generated by .kb/generate.mjs — do not edit by hand -->

Source: `apps/api/src/routes/v1/clinical-taxonomy.routes.ts`

> The clinical taxonomy: browsing the classification tree and curating it. The standard chain, and the order is the security model: requireTenant -> authenticate -> requireAuth -> authorize -> validate -> handler ── WHY READS ARE BEHIND `DOCTOR_READ` AND NOT THE MANAGE CODE ──────────────── Every screen that renders a doctor needs their specialty NAME. Gating the catalogue behind the permission to EDIT it means a receptionist sees blanks where the specialties should be. Same reasoning as `GET /doctors/masters`, which this surface complements rather than replaces. ── NO NEW PERMISSION CODES ────────────────────────────────────────────────── `DOCTOR_READ` and `DOCTOR_MASTER_MANAGE` both already…

| method | path | middleware → handler | at |
| --- | --- | --- | --- |
| USE | `*` | `requireTenant` → `authenticate` → `requireAuth` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:66` |
| GET | `/search` | `authorize(PERMISSIONS.DOCTOR_READ)` → `validate(taxonomySearchQuery)` → `<inline>` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:82` |
| GET | `/` | `authorize(PERMISSIONS.DOCTOR_READ)` → `validate(taxonomyTreeQuery)` → `<inline>` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:93` |
| GET | `/:nodeId/children` | `authorize(PERMISSIONS.DOCTOR_READ)` → `validate(nodeParams)` → `validate(taxonomyTreeQuery)` → `<inline>` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:103` |
| GET | `/:nodeId/tree` | `authorize(PERMISSIONS.DOCTOR_READ)` → `validate(nodeParams)` → `validate(taxonomyTreeQuery)` → `<inline>` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:118` |
| GET | `/:nodeId/ancestors` | `authorize(PERMISSIONS.DOCTOR_READ)` → `validate(nodeParams)` → `<inline>` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:131` |
| POST | `/` | `authorize(PERMISSIONS.DOCTOR_MASTER_MANAGE)` → `validate(createTaxonomyNodeRequest)` → `<inline>` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:143` |
| PATCH | `/:nodeId` | `authorize(PERMISSIONS.DOCTOR_MASTER_MANAGE)` → `validate(nodeParams)` → `validate(updateTaxonomyNodeRequest)` → `<inline>` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:157` |
| DELETE | `/:nodeId` | `authorize(PERMISSIONS.DOCTOR_MASTER_MANAGE)` → `validate(nodeParams)` → `<inline>` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:183` |

## Schemas declared here

| name | at |
| --- | --- |
| `nodeParams` | `apps/api/src/routes/v1/clinical-taxonomy.routes.ts:68` |

## Contracts imported from `@rcln/contracts`

`CreateTaxonomyNodeRequest` · `TaxonomySearchQuery` · `TaxonomyTreeQuery` · `UpdateTaxonomyNodeRequest` · `createTaxonomyNodeRequest` · `taxonomySearchQuery` · `taxonomyTreeQuery` · `updateTaxonomyNodeRequest`


Symbols in this module: [apps.api.routes.v1.md](../Symbols/apps.api.routes.v1.md)
