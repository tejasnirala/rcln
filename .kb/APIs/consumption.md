# API — `consumption`

<!-- generated by .kb/generate.mjs — do not edit by hand -->

Source: `apps/api/src/routes/v1/consumption.routes.ts`

> Clinical consumption: the templates, the panel and the record (PI-9). The standard chain, and the order IS the security model: requireTenant -> authenticate -> requireAuth -> authorize -> validate -> handler ── WHICH CODE GATES WHAT, AND WHY ────────────────────────────────────────── reading templates, the panel, the record -> `consumption.record.read` recording what was used -> `consumption.record` departing from the template's expectation -> `consumption.override` writing the templates themselves -> `consumption.template.manage` ⚠️ `consumption.override` IS ENFORCED IN THE SERVICE AND NOT BY `authorize`, AND IT IS THE ONE CODE ON THIS ROUTER THAT COULD NOT BE. Whether a body contains a va…

| method | path | middleware → handler | at |
| --- | --- | --- | --- |
| USE | `*` | `requireTenant` → `authenticate` → `requireAuth` | `apps/api/src/routes/v1/consumption.routes.ts:96` |
| GET | `/templates` | `authorize(CONSUMPTION_READ)` → `validate(consumptionTemplateQuery)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:142` |
| GET | `/templates/:id` | `authorize(CONSUMPTION_READ)` → `validate(idParams)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:152` |
| POST | `/templates` | `authorize(TEMPLATE_MANAGE)` → `validate(createConsumptionTemplateRequest)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:162` |
| PATCH | `/templates/:id` | `authorize(TEMPLATE_MANAGE)` → `validate(idParams)` → `validate(updateConsumptionTemplateRequest)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:179` |
| DELETE | `/templates/:id` | `authorize(TEMPLATE_MANAGE)` → `validate(idParams)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:204` |
| GET | `/plan` | `authorize(CONSUMPTION_READ)` → `validate(consumptionPlanQuery)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:230` |
| GET | `/records` | `authorize(CONSUMPTION_READ)` → `validate(consumptionQuery)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:251` |
| GET | `/records/:id` | `authorize(CONSUMPTION_READ)` → `validate(idParams)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:268` |
| POST | `/records` | `authorize(CONSUMPTION_RECORD)` → `validate(recordConsumptionRequest)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:292` |
| PATCH | `/records/:id` | `authorize(CONSUMPTION_RECORD)` → `validate(idParams)` → `validate(amendConsumptionRequest)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:317` |
| POST | `/records/:id/corrections` | `authorize(CONSUMPTION_RECORD)` → `validate(idParams)` → `validate(correctConsumptionRequest)` → `<inline>` | `apps/api/src/routes/v1/consumption.routes.ts:343` |

## Schemas declared here

| name | at |
| --- | --- |
| `idParams` | `apps/api/src/routes/v1/consumption.routes.ts:98` |

## Contracts imported from `@rcln/contracts`

`AmendConsumptionRequest` · `ConsumptionPlanQuery` · `ConsumptionQuery` · `ConsumptionTemplateQuery` · `CorrectConsumptionRequest` · `CreateConsumptionTemplateRequest` · `RecordConsumptionRequest` · `UpdateConsumptionTemplateRequest` · `amendConsumptionRequest` · `consumptionPlanQuery` · `consumptionQuery` · `consumptionTemplateQuery` · `correctConsumptionRequest` · `createConsumptionTemplateRequest` · `recordConsumptionRequest` · `updateConsumptionTemplateRequest`


Symbols in this module: [apps.api.routes.v1.md](../Symbols/apps.api.routes.v1.md)
