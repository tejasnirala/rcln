# API — `fees`

<!-- generated by .kb/generate.mjs — do not edit by hand -->

Source: `apps/api/src/routes/v1/fees.routes.ts`

> What the clinic charges for an appointment — the CLINIC's half of the grid. The standard chain, and the order is the security model: requireTenant -> authenticate -> requireAuth -> authorize -> validate -> handler ⚠️ A DOCTOR'S OVERRIDES ARE NOT HERE. They are `/v1/doctors/:doctorId/fees`, on the doctors router, because they hang off a practitioner and the same 404-if-invisible rule has to apply to the doctor id. Both surfaces call the same two service functions and the same single resolver; there is exactly one implementation of the precedence rule in the codebase. ⚠️ NOT PHI, WHICH IS WHY NOTHING HERE WRITES `data_access_logs`. A fee names a branch, a doctor, a kind of visit and a price —…

| method | path | middleware → handler | at |
| --- | --- | --- | --- |
| USE | `*` | `requireTenant` → `authenticate` → `requireAuth` | `apps/api/src/routes/v1/fees.routes.ts:56` |
| GET | `/quote` | `authorize(PERMISSIONS.FEE_SCHEDULE_READ)` → `validate(feeQuoteQuery)` → `<inline>` | `apps/api/src/routes/v1/fees.routes.ts:71` |
| GET | `/entries` | `authorize(PERMISSIONS.FEE_SCHEDULE_READ)` → `<inline>` | `apps/api/src/routes/v1/fees.routes.ts:82` |
| GET | `/` | `authorize(PERMISSIONS.FEE_SCHEDULE_READ)` → `validate(feeScheduleQuery)` → `<inline>` | `apps/api/src/routes/v1/fees.routes.ts:94` |
| PUT | `/` | `authorize(PERMISSIONS.FEE_SCHEDULE_MANAGE)` → `validate(setFeeScheduleRequest)` → `<inline>` | `apps/api/src/routes/v1/fees.routes.ts:116` |

## Contracts imported from `@rcln/contracts`

`FeeQuoteQuery` · `FeeScheduleQuery` · `SetFeeScheduleRequest` · `feeQuoteQuery` · `feeScheduleQuery` · `setFeeScheduleRequest`


Symbols in this module: [apps.api.routes.v1.md](../Symbols/apps.api.routes.v1.md)
