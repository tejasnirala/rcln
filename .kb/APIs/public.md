# API — `public`

<!-- generated by .kb/generate.mjs — do not edit by hand -->

Source: `apps/api/src/routes/v1/public.routes.ts`

> Public, unauthenticated routes served from the apex domain (rcln.com). Everything here is pre-tenant. `resolveTenant` runs app-wide and deliberately skips the lookup on the root domain, so `req.tenant` is absent by design — do NOT add `requireTenant` to this router, and do not reach for `withTenant`, because there is no tenant to scope to. That makes `@rcln/db/unsafe` correct here rather than a shortcut. It is the sanctioned pre-tenant escape hatch, and `demo_requests` is a table with no `organization_id` at all — see the exemption recorded in packages/db/prisma/rls/enable-rls.sql.

| method | path | middleware → handler | at |
| --- | --- | --- | --- |
| POST | `/demo-requests` | `publicFormLimiter` → `validate(demoRequestInput)` → `<inline>` | `apps/api/src/routes/v1/public.routes.ts:64` |
| GET | `/organizations/check-slug` | `slugCheckLimiter` → `validate(checkSlugQuery)` → `<inline>` | `apps/api/src/routes/v1/public.routes.ts:145` |
| POST | `/organizations/register` | `registrationLimiter` → `validate(registerOrganizationRequest)` → `<inline>` | `apps/api/src/routes/v1/public.routes.ts:170` |

## Contracts imported from `@rcln/contracts`

`CheckSlugQuery` · `DemoRequestInput` · `RegisterOrganizationRequest` · `RegisterOrganizationResponse` · `SlugAvailabilityResponse` · `checkSlugQuery` · `demoRequestInput` · `registerOrganizationRequest`


Symbols in this module: [apps.api.routes.v1.md](../Symbols/apps.api.routes.v1.md)
