# API — `roles`

<!-- generated by .kb/generate.mjs — do not edit by hand -->

Source: `apps/api/src/routes/v1/roles.routes.ts`

> Custom roles, on a clinic's own subdomain. The same chain as branches.routes.ts, in the same order, for the same reasons: requireTenant -> authenticate -> requireAuth -> authorize -> validate -> handler `iam.role.read` to look, `iam.role.manage` to write. Reading is separate because assigning a role means knowing what it grants, and `iam.role.read` is in BRANCH_ADMIN's list while `iam.role.manage` deliberately is not. `callerFrom(req)` rides alongside the tenant context on every write. The services need to know what the CALLER may do, not just which tenant they are in — see services/iam/guards.ts.

| method | path | middleware → handler | at |
| --- | --- | --- | --- |
| USE | `*` | `requireTenant` → `authenticate` → `requireAuth` | `apps/api/src/routes/v1/roles.routes.ts:40` |
| GET | `/` | `authorize(PERMISSIONS.IAM_ROLE_READ)` → `<inline>` | `apps/api/src/routes/v1/roles.routes.ts:57` |
| POST | `/` | `authorize(PERMISSIONS.IAM_ROLE_MANAGE)` → `validate(createRoleRequest)` → `<inline>` | `apps/api/src/routes/v1/roles.routes.ts:65` |
| PATCH | `/:roleId` | `authorize(PERMISSIONS.IAM_ROLE_MANAGE)` → `validate(roleParams)` → `validate(updateRoleRequest)` → `<inline>` | `apps/api/src/routes/v1/roles.routes.ts:85` |
| DELETE | `/:roleId` | `authorize(PERMISSIONS.IAM_ROLE_MANAGE)` → `validate(roleParams)` → `<inline>` | `apps/api/src/routes/v1/roles.routes.ts:109` |

## Schemas declared here

| name | at |
| --- | --- |
| `roleParams` | `apps/api/src/routes/v1/roles.routes.ts:42` |

## Contracts imported from `@rcln/contracts`

`CreateRoleRequest` · `UpdateRoleRequest` · `createRoleRequest` · `updateRoleRequest`


Symbols in this module: [apps.api.routes.v1.md](../Symbols/apps.api.routes.v1.md)
