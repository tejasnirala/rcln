# API — `tax`

<!-- generated by .kb/generate.mjs — do not edit by hand -->

Source: `apps/api/src/routes/v1/tax.routes.ts`

> The clinic's own tax configuration. The standard chain, and the order is the security model: requireTenant -> authenticate -> requireAuth -> authorize -> validate -> handler ⚠️ THIS IS NOT `/v1/platform/tax-registrations`, AND THE TWO MUST NEVER LEARN TO REACH EACH OTHER. That surface maintains rcln's own registrations and the catalogue of published rates every clinic inherits, behind `platform.tax.manage`. This one touches this organization's rows only, behind `billing.tax.read` / `.manage`, and every service call goes through `withTenant`. A clinic editing the catalogue would change what every clinic in the country charges. ⚠️ AND IT IS NOT A PHI SURFACE, WHICH IS WHY NOTHING HERE WRITES …

| method | path | middleware → handler | at |
| --- | --- | --- | --- |
| USE | `*` | `requireTenant` | `apps/api/src/routes/v1/tax.routes.ts:72` |
| USE | `*` | `authenticate` | `apps/api/src/routes/v1/tax.routes.ts:73` |
| USE | `*` | `requireAuth` | `apps/api/src/routes/v1/tax.routes.ts:74` |
| GET | `/rate-card` | `authorize(PERMISSIONS.BILLING_TAX_READ)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:96` |
| GET | `/registrations` | `authorize(PERMISSIONS.BILLING_TAX_READ)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:108` |
| POST | `/registrations` | `authorize(PERMISSIONS.BILLING_TAX_MANAGE)` → `validate(createClinicTaxRegistrationRequest)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:126` |
| PATCH | `/registrations/:id` | `authorize(PERMISSIONS.BILLING_TAX_MANAGE)` → `validate(clinicTaxParams)` → `validate(updateClinicTaxRegistrationRequest)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:140` |
| PUT | `/registrations/:id/branches` | `authorize(PERMISSIONS.BILLING_TAX_MANAGE)` → `validate(clinicTaxParams)` → `validate(setClinicTaxRegistrationBranchesRequest)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:170` |
| GET | `/rules` | `authorize(PERMISSIONS.BILLING_TAX_READ)` → `validate(listClinicTaxRulesQuery)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:191` |
| POST | `/rules` | `authorize(PERMISSIONS.BILLING_TAX_MANAGE)` → `validate(createClinicTaxRuleRequest)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:215` |
| PATCH | `/rules/:id` | `authorize(PERMISSIONS.BILLING_TAX_MANAGE)` → `validate(clinicTaxParams)` → `validate(updateClinicTaxRuleRequest)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:238` |
| POST | `/rules/:id/end` | `authorize(PERMISSIONS.BILLING_TAX_MANAGE)` → `validate(clinicTaxParams)` → `validate(endRuleBody)` → `<inline>` | `apps/api/src/routes/v1/tax.routes.ts:266` |

## Schemas declared here

| name | at |
| --- | --- |
| `endRuleBody` | `apps/api/src/routes/v1/tax.routes.ts:264` |

## Contracts imported from `@rcln/contracts`

`CreateClinicTaxRegistrationRequest` · `CreateClinicTaxRuleRequest` · `ListClinicTaxRulesQuery` · `SetClinicTaxRegistrationBranchesRequest` · `UpdateClinicTaxRegistrationRequest` · `UpdateClinicTaxRuleRequest` · `calendarDate` · `clinicTaxParams` · `createClinicTaxRegistrationRequest` · `createClinicTaxRuleRequest` · `listClinicTaxRulesQuery` · `setClinicTaxRegistrationBranchesRequest` · `updateClinicTaxRegistrationRequest` · `updateClinicTaxRuleRequest`


Symbols in this module: [apps.api.routes.v1.md](../Symbols/apps.api.routes.v1.md)
