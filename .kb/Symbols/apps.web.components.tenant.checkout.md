# `apps/web/components/tenant/checkout`

<!-- generated by .kb/generate.mjs — do not edit by hand -->

> The sandbox provider's "widget" — the same choices as its hosted page, inline.

Files: `apps/web/src/components/tenant/checkout/mock-checkout.tsx` · `apps/web/src/components/tenant/checkout/provider-checkout.tsx` · `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx`

## component

| name | signature | at | notes |
| --- | --- | --- | --- |
| `Adapter` <sub>local</sub> | `({ handoff, callbacks, }: { handoff: CheckoutHandoff; callba…)` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:251` |  |
| `CHOICES` <sub>local</sub> | `: { outcome: Outcome; label: string; note: string }[]` | `apps/web/src/components/tenant/checkout/mock-checkout.tsx:26` |  |
| `MockCheckout` | `({ handoff, callbacks, }: { handoff: CheckoutHandoff; callba…)` | `apps/web/src/components/tenant/checkout/mock-checkout.tsx:44` |  |
| `ProviderCheckout` | `({ handoff }: { handoff: CheckoutHandoff })` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:79` |  |
| `RazorpayCheckout` | `({ handoff, callbacks, }: { handoff: CheckoutHandoff; callba…)` | `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx:111` |  |
| `UnsupportedProvider` <sub>local</sub> | `({ name, onError }: { name: string; onError: (m: string) => …)` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:268` |  |

## fn

| name | signature | at | notes |
| --- | --- | --- | --- |
| `format` <sub>local</sub> | `(minor: number, currency: string): string` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:281` |  |
| `label` <sub>local</sub> | `(provider: string): string` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:292` | How a provider is named to a customer, who has never read our config. |
| `overlayDescription` <sub>local</sub> | `(handoff: CheckoutHandoff): string` | `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx:87` |  |
| `rate` <sub>local</sub> | `(bps: number): string` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:286` | `1800` -> `18%`. Display only; the arithmetic is all server-side. |
| `shortDate` <sub>local</sub> | `(iso: string): string` | `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx:63` | `2026-08-06` -> `6 Aug 2026`. Compact, because it shares one line. |

## const

| name | signature | at | notes |
| --- | --- | --- | --- |
| `CHECKOUT_SCRIPT` <sub>local</sub> | `'https://checkout.razorpay.com/v1/checkout.js'` | `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx:57` |  |
| `DESCRIPTION_LIMIT` <sub>local</sub> | `255` | `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx:60` | Razorpay truncates past this, and a cut-off summary reads as a bug. |

## interface

| name | signature | at | notes |
| --- | --- | --- | --- |
| `CheckoutCallbacks` | `{ onDone, onCancel, onError }` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:67` | What each adapter reports back. Success is "navigate now", nothing more. |
| `CheckoutHandoff` | `{ embedded, intentId, amountMinor, currency, purchase, customer }` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:44` |  |
| `RazorpayHandlerResponse` <sub>local</sub> | `{ razorpay_payment_id, razorpay_order_id, razorpay_signature }` | `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx:25` |  |
| `RazorpayInstance` <sub>local</sub> | `{ open, close, on }` | `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx:45` |  |
| `RazorpayOptions` <sub>local</sub> | `{ key, order_id, amount, currency, name, description, theme, prefill, retry, handler, modal }` | `apps/web/src/components/tenant/checkout/razorpay-checkout.tsx:31` |  |

## type

| name | signature | at | notes |
| --- | --- | --- | --- |
| `Outcome` <sub>local</sub> | `'SUCCEED' \| 'DECLINE' \| 'STALL'` | `apps/web/src/components/tenant/checkout/mock-checkout.tsx:24` |  |
| `Phase` <sub>local</sub> | `\| { state: 'open' } \| { state: 'cancelled' } \| { state: 'error'; message: string } \| { state: 'done' }` | `apps/web/src/components/tenant/checkout/provider-checkout.tsx:73` |  |
