# `apps/web`

<!-- generated by .kb/generate.mjs — do not edit by hand -->

> Standalone output is PRODUCTION-BUILD ONLY, and must never be on in dev.

Files: `apps/web/next.config.ts` · `apps/web/src/proxy.ts`

## fn

| name | signature | at | notes |
| --- | --- | --- | --- |
| `applyToRequest` <sub>local</sub> | `(request: NextRequest, renewal: Renewal): void` | `apps/web/src/proxy.ts:174` | Apply renewal to the request the render will see, before headers are copied. |
| `applyToResponse` <sub>local</sub> | `(response: NextResponse, renewal: Renewal): NextResponse` | `apps/web/src/proxy.ts:185` | Apply renewal to the response, which is what reaches the browser. |
| `clientAddress` <sub>local</sub> | `(request: NextRequest): Record<string, string>` | `apps/web/src/proxy.ts:97` | The browser's address, as a header to pass on — or nothing if we do not know it. A chain of proxies appends, so the original client is the first entry. |
| `getSubdomain` <sub>local</sub> | `(host: string): string \| null` | `apps/web/src/proxy.ts:77` |  |
| `proxy` | `(request: NextRequest): Promise<NextResponse>` | `apps/web/src/proxy.ts:202` |  |
| `renewSession` <sub>local</sub> | `(request: NextRequest, apiHost: string): Promise<Renewal>` | `apps/web/src/proxy.ts:125` |  |

## const

| name | signature | at | notes |
| --- | --- | --- | --- |
| `API_URL` <sub>local</sub> | `process.env['API_INTERNAL_URL'] ?? 'http://api:5000'` | `apps/web/src/proxy.ts:35` |  |
| `RESERVED` <sub>local</sub> | `new Set(…)` | `apps/web/src/proxy.ts:42` | Subdomains the platform keeps. A tenant claiming `api` or `admin` would break routing for everyone, so registration rejects these too — keep both lists in step… |
| `ROOT_DOMAIN` <sub>local</sub> | `process.env['NEXT_PUBLIC_ROOT_DOMAIN'] ?? 'lvh.me'` | `apps/web/src/proxy.ts:34` |  |

## var

| name | signature | at | notes |
| --- | --- | --- | --- |
| `config` | `{ // Skip API routes, Next internals, and anything with a file extension. matcher: ['/((?…` | `apps/web/src/proxy.ts:245` |  |
| `isStandaloneBuild` <sub>local</sub> | `process.env['NEXT_OUTPUT_STANDALONE'] === '1'` | `apps/web/next.config.ts:16` |  |
| `nextConfig` <sub>local</sub> | `: NextConfig` | `apps/web/next.config.ts:31` |  |
| `rootDomain` <sub>local</sub> | `process.env['NEXT_PUBLIC_ROOT_DOMAIN'] ?? 'lvh.me'` | `apps/web/next.config.ts:29` |  |

## type

| name | signature | at | notes |
| --- | --- | --- | --- |
| `Renewal` <sub>local</sub> | `\| { kind: 'none' } \| { kind: 'renewed'; accessToken: string; refreshToken: string; maxAge: number } \| { kind: 'spent' }` | `apps/web/src/proxy.ts:104` | What renewal decided, to be applied to whichever response we build. |
