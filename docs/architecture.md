# Moved

This document now lives at [`.kb/Architecture/architecture.md`](../.kb/Architecture/architecture.md).

The project documentation was consolidated into the `.kb/` KnowledgeBase so that
there is exactly one home for it. Infrastructure, deployment and third-party choices.

This stub exists only so older links keep resolving. Do not add content here.
